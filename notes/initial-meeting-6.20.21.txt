1. app skelton (build out initial framework in node) (follow up with Django for authentication i.e. user sign in)
2. python port optimizer code (ensure it works properly)
3. create a form to input tickers and generate python output from the data 
4. create dashboard with HTML/JS/CSS
5. historical options pricing
- have a form (with available tickers ... could use jquery to generate a list of the tickers)
- fetch data from a date range, once we hit the submit, calls from MS SQL database the first entry in the database (
- for ML/stats packages, there are js packages for this
